*** Begin Patch
*** Update File: src/commands/sync.ts
@@
 function git(repoPath: string, args: string[], configs: string[] = []): string {
-  return execFileSync('git', buildGitInvocation(repoPath, args, configs), {
-    encoding: 'utf-8',
-    timeout: 30000,
-    maxBuffer: 100 * 1024 * 1024,
-  }).trim();
+  // v0.42.57.1 (local patch): retry on transient spawn timeout.
+  // On Windows/MSYS, cold git spawns (DLL load, AV scan, parallel sync
+  // workers) intermittently exceed the 30s timeout even for trivial calls
+  // like `rev-parse HEAD` (which is ~0.1s when warm). A single flaky
+  // timeout was poisoning the HEAD-verification sentinel (<head>) and
+  // wedging sync. Retry up to 2x with backoff; keep the 30s per-call cap
+  // so genuine hangs still surface as failures.
+  const MAX_RETRIES = 2;
+  let lastErr: unknown;
+  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
+    try {
+      return execFileSync('git', buildGitInvocation(repoPath, args, configs), {
+        encoding: 'utf-8',
+        timeout: 30000,
+        maxBuffer: 100 * 1024 * 1024,
+      }).trim();
+    } catch (e) {
+      lastErr = e;
+      const err = e as NodeJS.ErrnoException & { code?: string; signal?: string };
+      const isTimeout = err?.code === 'ETIMEDOUT' || err?.signal === 'SIGTERM';
+      if (!isTimeout || attempt === MAX_RETRIES) break;
+      const backoff = 500 * (attempt + 1);
+      console.warn(
+        `[git retry ${attempt + 1}/${MAX_RETRIES}] ${args.join(' ')} in ${repoPath} ` +
+          `timed out; retrying in ${backoff}ms`,
+      );
+      // busy-wait backoff (sync helper, no async in caller path)
+      const start = Date.now();
+      while (Date.now() - start < backoff) { /* spin */ }
+    }
+  }
+  throw lastErr;
 }
*** End Patch
